<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sicnu.boot.mapper.MenuMapper">
    <select id="getMenuList" resultType="Menu">
        select * from menu
    </select>
    <select id="checkMenuByMenuId" resultType="java.lang.Integer">
        select count(*) from menu where menu_id = #{menuId}
    </select>

    <insert id="insertOneMenu" parameterType="Menu">
        insert into menu values(null,#{path},#{name},#{component},null,1,1)
    </insert>

    <insert id="insertChildrenMenu" parameterType="Menu">
        insert into menu values(null,#{path},#{name},#{component},#{parentId},#{menuType},#{level})
    </insert>

    <update id="updateMenu" parameterType="Menu">
        update menu set path = #{path} and component = #{component} and name = #{name}
            where menu_id = #{menuId}
    </update>

    <delete id="deleteMenuByMenuId">
        delete from menu where menu_id = #{menuId}
    </delete>

</mapper>