<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sicnu.boot.mapper.PowerMapper">
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.`perms`
        FROM
            user_role ur
                LEFT JOIN `role` r ON ur.`role_id` = r.`role_id`
                LEFT JOIN `role_power` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `power` m ON m.`power_id` = rm.`power_id`
        WHERE
            user_id = #{userId}
    </select>
</mapper>